{
  "name": "hometracker",
  "version": "1.0.0",
  "description": "Application de gestion de maison",
  "scripts": {
    "backend": "cd backend && pnpm dev",
    "frontend": "cd frontend && pnpm dev",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
